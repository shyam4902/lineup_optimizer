# Remaining work

September 11: the current V1 scope and prompts are in `docs/superpowers/plans/2026-09-11-v1-lineup-workspace.md` and `V1_AGENT_PROMPTS.md`. The prompts below are historical and must not expand the current workspace task into strategy optimization or publishing.

September 9 update: a password-protected temporary preview now runs in a separate workspace for one friend's own cards and lineups. See `SHARING.md` for the active link, runtime location, verification, and limits. Save Plan now includes scores and kickoff times. The permanent hosting prompt below remains future work; the user has confirmed that separate personal workspaces are required.

The local app now imports kickoff times from the NFL dashboard's `props-board.json` games array. In Live Lineup Manager, select season and week and use **Import dashboard kickoff times**. It fills missing times, rejects conflicting existing times, preserves scores, and invalidates old swap recommendations. It reads the sibling `NFL_Main/nfldashboard` checkout by default. `NFL_DASHBOARD_BOARD_PATH` can override that file location. Import again after restarting the server. This change does not fetch live scores, repair saved lineups, or publish either app.

## Prompt 1: Finish the real-data homepage

Make the GameBlazers football app useful for managing my real Week 1 lineups during games. Start in `/Users/shyampatel/Desktop/GB/lineup_optimizer`, read `/Users/shyampatel/Desktop/GB/docs/football-live-lineup-strategy.md`, and preserve the existing optimizer logic and other agents' uncommitted work. The goal is complete when the home page prioritizes my lineups, banked points, remaining players, salary flexibility, next kickoff, and actionable coordinated swaps; demos move out of the normal workflow; and the app clearly distinguishes incomplete setup from eliminated lineups. Investigate why all 22 cards in the saved four-lineup plan fail to match the current roster, without guessing replacements by player name or overwriting that file. Verify the real roster and selected week in the browser, along with the existing test suite. Report any actual entered-lineup data that still needs to come from me.

## Prompt 2: Publish and connect the apps

Publish the existing GameBlazers football app under my Cloudflare-managed custom domain, then add a launcher card and an NFL dashboard button pointing to it. First identify and verify my actual domain, hosting account, and deployed launcher. The local launcher source is `/Users/shyampatel/Desktop/Live Apps/Home/index.html`; the NFL dashboard repo is `/Users/shyampatel/Desktop/NFL_Main/nfldashboard`. Follow each repo's instructions. Keep the Python/PuLP solver working, protect private roster and lineup writes, and persist plans, actual scores, and selected-week kickoff data across restarts. The current app uses one global state and a single saved-plan file, so decide explicitly whether this is one shared private workspace or separate user accounts before exposing it. Reuse the dashboard's existing schedule export with visible season/week and data timestamps, and reject missing or mismatched data. Do not substitute season-long projections for weekly fantasy projections. The goal is complete when the custom URL and both navigation links work on a phone, a saved lineup survives a restart, and a real-data kickoff/import/swap check passes. Avoid a framework rewrite; verify the solver's hosting requirements before choosing a runtime. Keep my Mac's sleep state from becoming a hidden production dependency.
